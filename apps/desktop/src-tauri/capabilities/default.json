{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Minimum capabilities the shell needs. Deliberately short: every entry here is a permission granted to the webview, so the list is reviewed rather than grown by habit.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:allow-open"
  ]
}
