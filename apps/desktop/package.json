{
  "name": "easypdf-desktop",
  "private": true,
  "version": "0.0.0-dev",
  "type": "module",
  "description": "EasyPDF desktop shell",
  "license": "Apache-2.0",
  "scripts": {
    "build": "esbuild src/main.ts --bundle --format=esm --target=es2022 --outfile=dist/main.js && node scripts/copy-static.mjs",
    "dev": "tauri dev",
    "tauri": "tauri",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2",
    "esbuild": "^0.28.2",
    "typescript": "^5"
  },
  "dependencies": {
    "@tauri-apps/api": "^2",
    "@tauri-apps/plugin-dialog": "^2.7.2"
  }
}
